name: BMLB2025_2026
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip

  # Core scientific stack
  - numpy
  - scipy
  - pandas
  - scikit-learn
  - statsmodels

  # Viz & notebooks
  - matplotlib
  - seaborn
  - jupyterlab
  - ipykernel
  - ipympl
  - pydot

  # ML libraries
  - keras>=3
  - imbalanced-learn
  - scikeras

  # Utilities
  - tqdm

  # Experiment tracking
  - mlflow

  # For smarter notebook merge handling
  - nbdev>=2.3.0
  - gitpython

  # Automate markdown to pdf rendering
  - pandoc
  - tectonic
  - pre-commit
  - nbformat

  # Pip-only packages (PyTorch)
  - pip:
      - torch
      - torchvision